apiVersion: v1
kind: Service
metadata:
  name: mcp-server
  namespace: agentic-analytics
spec:
  selector:
    app: mcp-server
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
