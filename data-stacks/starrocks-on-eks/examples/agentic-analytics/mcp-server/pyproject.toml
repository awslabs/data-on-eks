[project]
name = "agentic-analytics-mcp-server"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "mcp-server-starrocks==0.3.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
