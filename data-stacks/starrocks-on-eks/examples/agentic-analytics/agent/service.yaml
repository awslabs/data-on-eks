apiVersion: v1
kind: Service
metadata:
  name: agent
  namespace: agentic-analytics
spec:
  selector:
    app: agent
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
