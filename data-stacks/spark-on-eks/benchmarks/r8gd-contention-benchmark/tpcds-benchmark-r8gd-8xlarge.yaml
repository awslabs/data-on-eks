# TPC-DS Benchmark for r8gd.8xlarge - Single Pod Per Node Configuration
#
# PURPOSE: Baseline measurement for comparison against r8gd.24xlarge multi-pod configuration
#
# Configuration:
# - Instance: r8gd.8xlarge (32 vCPU, 256 GiB, 1x1900GB NVMe)
# - Executors: 24 total, 1 per node
# - Resources per executor: 31 vCPU, 200GB memory
# - Total: 744 vCPU, 4800GB memory across 24 nodes
#
# Expected behavior:
# - No CPU cache contention (single JVM per node)
# - No NUMA cross-node memory access contention
# - Full L3 cache available to single executor
# - Full memory bandwidth to single process
#
# Before running:
# 1. Apply EC2NodeClass and NodePool: kubectl apply -f ec2nodeclass-r8gd-benchmark.yaml nodepool-r8gd-8xlarge.yaml
# 2. Replace <S3_BUCKET> with your bucket (e.g., <S3_BUCKET>)
# 3. Update namespace if needed

---
apiVersion: "sparkoperator.k8s.io/v1beta2"
kind: SparkApplication
metadata:
  name: tpcds-benchmark-r8gd-8xlarge
  namespace: spark-team-a
  labels:
    benchmark: r8gd-contention
    instance-type: r8gd-8xlarge
    pods-per-node: "1"
spec:
  type: Scala
  mode: cluster
  image: public.ecr.aws/data-on-eks/spark3.5.3-scala2.12-java17-python3-ubuntu-tpcds:v2
  imagePullPolicy: IfNotPresent
  sparkVersion: 3.5.3
  mainClass: com.amazonaws.eks.tpcds.BenchmarkSQL
  mainApplicationFile: local:///opt/spark/examples/jars/eks-spark-benchmark-assembly-1.0.jar
  arguments:
    # TPC-DS data location (1TB dataset)
    - "s3a://<S3_BUCKET>/TPCDS-TEST-1TB"
    # Results location
    - "s3a://<S3_BUCKET>/benchmark-results/r8gd-contention/r8gd-8xlarge"
    # Path to TPC-DS kit
    - "/opt/tpcds-kit/tools"
    # Data format
    - "parquet"
    # Scale factor (1TB)
    - "1000"
    # Number of iterations
    - "3"
    # Optimize with hive tables
    - "false"
    # Filter queries (empty = all queries)
    - ""
    # Logging level WARN
    - "true"

  sparkConf:
    # === IDENTIFICATION ===
    "spark.app.name": "tpcds-r8gd-8xlarge-1pod-per-node"
    "spark.kubernetes.driver.pod.name": "tpcds-r8gd-8xlarge-driver"
    "spark.kubernetes.executor.podNamePrefix": "tpcds-r8gd-8xlarge-exec"

    # === LOCAL STORAGE (NVMe SSD) ===
    "spark.local.dir": "/data1"
    "spark.shuffle.file.buffer": "1m"
    "spark.shuffle.spill.diskWriteBufferSize": "1048576"
    "spark.io.compression.codec": "lz4"
    "spark.shuffle.compress": "true"
    "spark.shuffle.spill.compress": "true"
    "spark.rdd.compress": "true"
    "spark.reducer.maxSizeInFlight": "96m"

    # === ADAPTIVE QUERY EXECUTION ===
    "spark.sql.adaptive.enabled": "true"
    "spark.sql.adaptive.localShuffleReader.enabled": "true"
    "spark.sql.adaptive.coalescePartitions.enabled": "true"
    "spark.sql.adaptive.skewJoin.enabled": "true"
    "spark.sql.adaptive.skewJoin.skewedPartitionFactor": "5"
    "spark.sql.adaptive.advisoryPartitionSizeInBytes": "256m"

    # === PARALLELISM (tuned for 24 executors x 31 cores = 744 cores) ===
    "spark.sql.shuffle.partitions": "2000"
    "spark.default.parallelism": "1488"
    "spark.sql.files.maxPartitionBytes": "256m"

    # === MEMORY TUNING ===
    "spark.memory.fraction": "0.8"
    "spark.memory.storageFraction": "0.3"
    "spark.sql.autoBroadcastJoinThreshold": "256m"

    # === JVM TUNING (critical for performance comparison) ===
    # Using G1GC which is better for large heaps on Graviton
    "spark.executor.defaultJavaOptions": >-
      -XX:+UseG1GC
      -XX:+UnlockExperimentalVMOptions
      -XX:G1HeapRegionSize=32m
      -XX:InitiatingHeapOccupancyPercent=35
      -XX:ConcGCThreads=8
      -XX:ParallelGCThreads=16
      -XX:+ParallelRefProcEnabled
      -XX:+AlwaysPreTouch
      -XX:+UseNUMA
      -XX:+UseTransparentHugePages
      -XX:+PerfDisableSharedMem
      -Djdk.nio.maxCachedBufferSize=2000000
      -verbose:gc
      -Xlog:gc*:file=/tmp/gc.log:time,uptime:filecount=5,filesize=100m
    "spark.driver.defaultJavaOptions": >-
      -XX:+UseG1GC
      -XX:InitiatingHeapOccupancyPercent=35
      -XX:+AlwaysPreTouch
      -verbose:gc

    # === S3 CONFIGURATION ===
    "spark.hadoop.fs.s3a.aws.credentials.provider": "software.amazon.awssdk.auth.credentials.ContainerCredentialsProvider"
    "spark.hadoop.fs.s3.impl": "org.apache.hadoop.fs.s3a.S3AFileSystem"
    "spark.hadoop.fs.s3a.fast.upload": "true"
    "spark.hadoop.fs.s3a.path.style.access": "true"
    "spark.hadoop.fs.s3a.fast.upload.buffer": "disk"
    "spark.hadoop.fs.s3a.buffer.dir": "/data1/s3a"
    "spark.hadoop.fs.s3a.multipart.size": "128M"
    "spark.hadoop.fs.s3a.multipart.threshold": "256M"
    "spark.hadoop.fs.s3a.threads.max": "50"
    "spark.hadoop.fs.s3a.connection.maximum": "200"
    "spark.hadoop.fs.s3a.readahead.range": "256K"
    "spark.hadoop.mapreduce.fileoutputcommitter.algorithm.version": "2"

    # === EVENT LOGGING ===
    "spark.eventLog.enabled": "true"
    "spark.eventLog.dir": "s3a://S3_BUCKET/spark-event-logs"
    "spark.eventLog.rolling.enabled": "true"
    "spark.eventLog.rolling.maxFileSize": "64m"

    # === PROMETHEUS METRICS ===
    "spark.ui.prometheus.enabled": "true"
    "spark.executor.processTreeMetrics.enabled": "true"
    "spark.metrics.conf.*.sink.prometheusServlet.class": "org.apache.spark.metrics.sink.PrometheusServlet"
    "spark.metrics.conf.driver.sink.prometheusServlet.path": "/metrics/driver/prometheus/"
    "spark.metrics.conf.executor.sink.prometheusServlet.path": "/metrics/executors/prometheus/"

    # === NETWORK TIMEOUT ===
    "spark.network.timeout": "2000s"
    "spark.executor.heartbeatInterval": "300s"
    "spark.kubernetes.submission.connectionTimeout": "120000"
    "spark.kubernetes.submission.requestTimeout": "120000"
    "spark.kubernetes.driver.connectionTimeout": "120000"
    "spark.kubernetes.driver.requestTimeout": "120000"

    # === SPECULATION (disable for benchmark consistency) ===
    "spark.speculation": "false"

  # === DRIVER CONFIGURATION ===
  driver:
    cores: 5
    memory: "20g"
    memoryOverhead: "4g"
    serviceAccount: spark-team-a
    securityContext:
      runAsUser: 185
    annotations:
      yunikorn.apache.org/queue: root.spark.spark-team-a
    labels:
      benchmark: r8gd-contention
      component: driver
    # Driver on separate node (not r8gd to avoid skewing results)
    nodeSelector:
      node.kubernetes.io/workload-type: memory-optimized-graviton
    volumeMounts:
      - name: spark-local-dir-1
        mountPath: /data1
    initContainers:
      - name: volume-permission
        image: public.ecr.aws/docker/library/busybox
        command: ['sh', '-c', 'mkdir -p /data1/s3a; chown -R 185:185 /data1']
        volumeMounts:
          - name: spark-local-dir-1
            mountPath: /data1
    env:
      - name: JAVA_HOME
        value: "/opt/java/openjdk"

  # === EXECUTOR CONFIGURATION ===
  executor:
    # 31 cores per executor = 1 executor per r8gd.8xlarge (32 vCPU)
    cores: 31
    # 200GB memory (leaving headroom for OS, kubelet, and overhead)
    memory: "200g"
    memoryOverhead: "20g"
    # 24 executors = 24 nodes
    instances: 24 # 12 nodes for next test
    serviceAccount: spark-team-a
    securityContext:
      runAsUser: 185
    annotations:
      yunikorn.apache.org/queue: root.spark.spark-team-a
    labels:
      benchmark: r8gd-contention
      component: executor
      instance-type: r8gd-8xlarge
    # Node selector with toleration for benchmark taint
    nodeSelector:
      node.kubernetes.io/workload-type: r8gd-benchmark-8xlarge
    tolerations:
      - key: benchmark
        operator: Equal
        value: r8gd-8xlarge
        effect: NoSchedule
    volumeMounts:
      - name: spark-local-dir-1
        mountPath: /data1
    initContainers:
      - name: volume-permission
        image: public.ecr.aws/docker/library/busybox
        command: ['sh', '-c', 'mkdir -p /data1/s3a; chown -R 185:185 /data1']
        volumeMounts:
          - name: spark-local-dir-1
            mountPath: /data1
    env:
      - name: JAVA_HOME
        value: "/opt/java/openjdk"

  restartPolicy:
    type: Never

  volumes:
    - name: spark-local-dir-1
      hostPath:
        path: "/mnt/k8s-disks/0"
        type: DirectoryOrCreate
